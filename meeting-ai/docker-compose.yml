version: "3.9"
services:
  meetingai:
    build: .
    ports:
      - "8501:8501"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
    volumes:
      - ./:/app
    restart: unless-stopped
